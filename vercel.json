{
  "version": 2,
  "routes": [
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ],
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "dist/tu-app-name",
  "framework": "angular"
}